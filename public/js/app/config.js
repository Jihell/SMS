(function($) {

    App.config = {
        /**
         * Selector list for jQuery
         */
        selector: {
            form: '#chatForm',
            chat: '#tchat',
            message: '#message',
            pseudo: '#nick',
            roomTopic: '#global h2',
            roomName: '#global h1',
            hiddenOverlay: '#hiddenOverlay',
            audio: '#audio'
        },
        title: 'SMS',
        sound: true
    }

})(jQuery);